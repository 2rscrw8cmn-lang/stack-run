# Server-only Connected Training secrets.
# Copy to .env.local for local testing and fill values there.
# Never prefix either variable with VITE_.
# Never commit real values.

INTERVALS_API_KEY=
STACK_SYNC_TOKEN=
