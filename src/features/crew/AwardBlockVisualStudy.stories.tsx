// Visual fixture intentionally has no Storybook dependency.
// Import `AwardBlockVisualStudyRoute` from a temporary dev-only entrypoint if
// you want to mount this study in the app shell while reviewing the branch.
export { AwardBlockVisualStudyRoute as default } from "./awardBlockVisualStudyRoute";
